# BearSpeak

Instructions for using Git:

setting up:
git clone https://github.com/af39/BearSpeak.git

*first time, you will have to also do “git init”

making changes:
git commit -am “message”
git push origin master <- when you’re ready to push for others to pull

grabbing other people’s changes:
git pull
